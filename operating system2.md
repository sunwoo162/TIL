# 운영체제 2
버스
```
컴퓨터 구조에서 각 장치간 데이터의 이동 통로
```
메인보드
```
CPU와 메모리 등 다양한 부품을 연결하고 전원을 공급해 주는 커다란 판
```
폰노이만 구조
```
폰노이만 구조에서 모든 프로그램은 메모리에 올라와야 실행할 수 있다
```
메모리
```
요리사 모형의 도마(작업 공간)와 가장 관련이 높은 것은 메모리다(다른 프린트 참고)
```
헤르츠
```
CPU의 성능을 나타내는 단위로서 1초당 변화를 나타낼 때 사용되는 단위
```
CPU구성 요소
```
산술 논리 연산장치(연산을 담당)
제어장치
레지스터
```
메모리 주소 레지스터(MAR)
```
데이터를 가져오거나 반대로 메모리로 데이터를 보낼 때 주소를 지정하기 위해 사용하는 레지스터
```
스풀
```
입출력장치가 독립적으로 동작하도록 고안된 소프트웨어적인 버퍼
```
캐시
```
앞으로 사용할 것을 미리 가져와 속도를 향상하는 장치
```
캐시 히트
```
캐시에 원하는 데이터가 있는 경우를 나타내는 것
```
캐시 미스
```
캐시에 원하는 데이터가 없는 경우를 타나태는 것
```
저장 장치 계층 구조
```
저렴한 가격으로 속도는 레지스터처럼, 용량은 하드디스크처럼 시스템을 구성해 주는 것
```
인터럽트
```
CPU 작업과 입출력 작업을 분리하여 효율을 높이는 기법
```
MMIO
```
메모리의 일정 공간을 입출력에 할당하는 기법
```
직접 메로리 접근
```
입출력 관리자에게 CPU의 허락 없이 메모리에 접근할 수 있게 하는 권한
```
컴퓨터 필수장치
```
CPU와 메모리
```
컴파일
```
프로그래밍 언어로 작성된 소스코드를 컴퓨터가 실행할 수 있는 기계어로 번역하여 실행 파일을 만든 후 한꺼번에 실행하는 것
```
프로그램 카운터
```
다음에 실행할 주소를 가진 레지스터
```
명령어 레지스터
```
현재 실행 중인 명령어를 저장한 레지스터
```
메모리 주소 레지스터(MAR)
```
데이터를 가져오거나 반대로 메모리로 데이터를 보낼 때 주소를 지정하기 위해 사용하는 레지스터
```
메모리 버퍼 레지스터(MBR)
```
메모리에서 가져온 데이터나 메모리로 옮겨 갈 데이터를 임시로 저장하는 레지스터
```
시스템 버스(FSB)
```
제어 버스
주소 버스
데이터 버스 로 구성
```
주소 버스
```
시스템 버스 중 단방향 버스이다
```
참고
```
시스템 버스 중 제어 버스는 CPU의 제어장치와 연결된다
```
```
시스템 버스 중 주소 버스는 CPU의 메모리 주소 레지스터와 연결된다
```
```
시스템 버스 중데이터 버스는 CPU의 메모리 버퍼 레지스터와 연결된다
```
```
부트스트랩은 부팅 과정에서 운영체제를 실행하기 위한 작은 코드
```
```
버퍼는 두 장치 사이에서 속도의 차이를 완화하는 역할을 한다
```
```
스풀은 입출력장치가 독립적으로 동작하도록 고안된 소프트웨어적인 버퍼다
```
